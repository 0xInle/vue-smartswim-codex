--- .codex-backups/home-performance-20260401/HomeHeroSection.vue	2026-04-01 12:57:46
+++ src/pages/home/components/HomeHeroSection.vue	2026-04-01 12:59:24
@@ -14,7 +14,7 @@
     </div>
     <div class="home__main flex">
       <div class="home__video">
-        <video autoplay muted loop playsinline class="home__video-player">
+        <video autoplay muted loop playsinline preload="metadata" class="home__video-player">
           <source src="/videos/01-video.mp4" type="video/mp4" />
           Ваш браузер не поддерживает видео.
         </video>
--- .codex-backups/home-performance-20260401/HomeReviewsSection.vue	2026-04-01 12:57:46
+++ src/pages/home/components/HomeReviewsSection.vue	2026-04-01 12:58:40
@@ -2,10 +2,13 @@
   <section class="reviews">
     <div class="container">
       <div
+        ref="carouselRef"
         class="reviews__carousel"
         tabindex="0"
         @mouseenter="pauseAutoplay"
         @mouseleave="startAutoplay"
+        @focusin="pauseAutoplay"
+        @focusout="startAutoplay"
         @keydown.left.prevent="goToPrevious"
         @keydown.right.prevent="goToNext"
       >
@@ -15,7 +18,7 @@
             :key="review.name"
             class="reviews__card"
             :class="{ 'reviews__card--active': index === activeIndex }"
-            :style="getCardStyle(index)"
+            :style="cardStyles[index]"
           >
             <p class="reviews__text">{{ review.text }}</p>
             <div class="reviews__meta">
@@ -50,7 +53,7 @@
 </template>
 
 <script setup>
-import { onBeforeUnmount, onMounted, ref } from 'vue'
+import { computed, onBeforeUnmount, onMounted, ref } from 'vue'
 
 const reviews = [
   {
@@ -116,7 +119,11 @@
 ]
 
 const activeIndex = ref(0)
+const carouselRef = ref(null)
+const isCarouselVisible = ref(true)
+const autoplayDelay = 2600
 let autoplayId = null
+let visibilityObserver = null
 
 const getRelativeIndex = (index) => {
   const total = reviews.length
@@ -133,41 +140,43 @@
   return difference
 }
 
-const getCardStyle = (index) => {
-  const relativeIndex = getRelativeIndex(index)
-  const absIndex = Math.abs(relativeIndex)
+const transforms = {
+  0: 'translateX(-50%) translateZ(0px) scale(1)',
+  1: 'translateX(calc(-50% + 270px)) rotateY(-12deg) scale(0.92)',
+  '-1': 'translateX(calc(-50% - 270px)) rotateY(12deg) scale(0.92)',
+  2: 'translateX(calc(-50% + 470px)) rotateY(-18deg) scale(0.82)',
+  '-2': 'translateX(calc(-50% - 470px)) rotateY(18deg) scale(0.82)',
+}
 
-  if (absIndex > 2) {
-    return {
-      opacity: 0,
-      pointerEvents: 'none',
-      transform: 'translateX(-50%) scale(0.84)',
-      zIndex: 0,
-    }
-  }
+const opacityMap = {
+  0: 1,
+  1: 0.72,
+  '-1': 0.72,
+  2: 0.34,
+  '-2': 0.34,
+}
 
-  const transforms = {
-    0: 'translateX(-50%) translateZ(0px) scale(1)',
-    1: 'translateX(calc(-50% + 270px)) rotateY(-12deg) scale(0.92)',
-    '-1': 'translateX(calc(-50% - 270px)) rotateY(12deg) scale(0.92)',
-    2: 'translateX(calc(-50% + 470px)) rotateY(-18deg) scale(0.82)',
-    '-2': 'translateX(calc(-50% - 470px)) rotateY(18deg) scale(0.82)',
-  }
+const cardStyles = computed(() =>
+  reviews.map((_, index) => {
+    const relativeIndex = getRelativeIndex(index)
+    const absIndex = Math.abs(relativeIndex)
 
-  const opacityMap = {
-    0: 1,
-    1: 0.72,
-    '-1': 0.72,
-    2: 0.34,
-    '-2': 0.34,
-  }
+    if (absIndex > 2) {
+      return {
+        opacity: 0,
+        pointerEvents: 'none',
+        transform: 'translateX(-50%) scale(0.84)',
+        zIndex: 0,
+      }
+    }
 
-  return {
-    opacity: opacityMap[relativeIndex],
-    transform: transforms[relativeIndex],
-    zIndex: 10 - absIndex,
-  }
-}
+    return {
+      opacity: opacityMap[relativeIndex],
+      transform: transforms[relativeIndex],
+      zIndex: 10 - absIndex,
+    }
+  }),
+)
 
 const goToReview = (index) => {
   activeIndex.value = index
@@ -192,21 +201,61 @@
 }
 
 const startAutoplay = () => {
+  if (!isCarouselVisible.value || document.hidden) {
+    return
+  }
+
   pauseAutoplay()
   autoplayId = window.setInterval(() => {
     activeIndex.value = (activeIndex.value + 1) % reviews.length
-  }, 2600)
+  }, autoplayDelay)
 }
 
 const restartAutoplay = () => {
   startAutoplay()
 }
 
+const handleVisibilityChange = () => {
+  if (document.hidden) {
+    pauseAutoplay()
+    return
+  }
+
+  startAutoplay()
+}
+
 onMounted(() => {
+  if (window.IntersectionObserver && carouselRef.value) {
+    visibilityObserver = new window.IntersectionObserver(
+      ([entry]) => {
+        isCarouselVisible.value = entry?.isIntersecting ?? false
+
+        if (isCarouselVisible.value) {
+          startAutoplay()
+          return
+        }
+
+        pauseAutoplay()
+      },
+      {
+        threshold: 0.2,
+      },
+    )
+
+    visibilityObserver.observe(carouselRef.value)
+  }
+
+  document.addEventListener('visibilitychange', handleVisibilityChange)
   startAutoplay()
 })
 
 onBeforeUnmount(() => {
+  document.removeEventListener('visibilitychange', handleVisibilityChange)
+
+  if (visibilityObserver) {
+    visibilityObserver.disconnect()
+  }
+
   pauseAutoplay()
 })
 </script>
@@ -215,6 +264,9 @@
 .reviews {
   padding: 30px 0 140px;
   overflow: hidden;
+  content-visibility: auto;
+  contain: layout paint style;
+  contain-intrinsic-size: 560px;
 }
 
 .reviews__carousel {
--- .codex-backups/home-performance-20260401/HomeAdvantagesSection.vue	2026-04-01 12:57:46
+++ src/pages/home/components/HomeAdvantagesSection.vue	2026-04-01 12:59:28
@@ -1,7 +1,7 @@
 <template>
   <section class="advantages">
     <div class="advantages__video">
-      <video autoplay muted loop playsinline class="advantages__video-player">
+      <video autoplay muted loop playsinline preload="metadata" class="advantages__video-player">
         <source src="/videos/02-video.mp4" type="video/mp4" />
         Ваш браузер не поддерживает видео.
       </video>
@@ -9,19 +9,21 @@
       <div class="advantages__overlay">
         <div class="advantages__grid">
           <div
+            :ref="(element) => setCardRef(element, 0)"
             class="advantages__card advantages__card--wide"
-            :style="{ transform: cardTransforms[0] }"
-            @mousemove="updateCardTilt($event, 0)"
-            @mouseleave="resetCardTilt(0)"
+            @pointerenter="cacheCardRect($event, 0)"
+            @pointermove="updateCardTilt($event, 0)"
+            @pointerleave="resetCardTilt(0)"
           >
             <span class="advantages__text">Квалифицированные специалисты</span>
           </div>
 
           <div
+            :ref="(element) => setCardRef(element, 1)"
             class="advantages__card advantages__card--tall"
-            :style="{ transform: cardTransforms[1] }"
-            @mousemove="updateCardTilt($event, 1)"
-            @mouseleave="resetCardTilt(1)"
+            @pointerenter="cacheCardRect($event, 1)"
+            @pointermove="updateCardTilt($event, 1)"
+            @pointerleave="resetCardTilt(1)"
           >
             <span class="advantages__text">
               Программы тренировок для всех возрастов и уровней подготовки
@@ -29,37 +31,41 @@
           </div>
 
           <div
+            :ref="(element) => setCardRef(element, 2)"
             class="advantages__card"
-            :style="{ transform: cardTransforms[2] }"
-            @mousemove="updateCardTilt($event, 2)"
-            @mouseleave="resetCardTilt(2)"
+            @pointerenter="cacheCardRect($event, 2)"
+            @pointermove="updateCardTilt($event, 2)"
+            @pointerleave="resetCardTilt(2)"
           >
             <span class="advantages__text">Проведение соревнований с присуждением разрядов</span>
           </div>
 
           <div
+            :ref="(element) => setCardRef(element, 3)"
             class="advantages__card advantages__card--accent"
-            :style="{ transform: cardTransforms[3] }"
-            @mousemove="updateCardTilt($event, 3)"
-            @mouseleave="resetCardTilt(3)"
+            @pointerenter="cacheCardRect($event, 3)"
+            @pointermove="updateCardTilt($event, 3)"
+            @pointerleave="resetCardTilt(3)"
           >
             <span class="advantages__text"> 15 лет успешной работы </span>
           </div>
 
           <div
+            :ref="(element) => setCardRef(element, 4)"
             class="advantages__card"
-            :style="{ transform: cardTransforms[4] }"
-            @mousemove="updateCardTilt($event, 4)"
-            @mouseleave="resetCardTilt(4)"
+            @pointerenter="cacheCardRect($event, 4)"
+            @pointermove="updateCardTilt($event, 4)"
+            @pointerleave="resetCardTilt(4)"
           >
             <span class="advantages__text">Индивидуальный подход к каждому</span>
           </div>
 
           <div
+            :ref="(element) => setCardRef(element, 5)"
             class="advantages__card advantages__card--wide"
-            :style="{ transform: cardTransforms[5] }"
-            @mousemove="updateCardTilt($event, 5)"
-            @mouseleave="resetCardTilt(5)"
+            @pointerenter="cacheCardRect($event, 5)"
+            @pointermove="updateCardTilt($event, 5)"
+            @pointerleave="resetCardTilt(5)"
           >
             <span class="advantages__text">Регулярные тренировочные сборы</span>
           </div>
@@ -70,14 +76,54 @@
 </template>
 
 <script setup>
-import { ref } from 'vue'
+import { onBeforeUnmount, onMounted, ref } from 'vue'
 
 const defaultTransform = 'translate3d(0px, 0px, 0px) rotateX(0deg) rotateY(0deg) scale(1)'
-const cardTransforms = ref(Array(6).fill(defaultTransform))
+const cardRefs = ref([])
+const cardRects = []
+const pendingTransforms = Array(6).fill(defaultTransform)
+const frameIds = Array(6).fill(0)
+let supportsInteractiveTilt = false
+
+const setCardRef = (element, index) => {
+  cardRefs.value[index] = element
+
+  if (element) {
+    element.style.transform = defaultTransform
+  }
+}
+
+const scheduleTransformUpdate = (index) => {
+  if (frameIds[index]) {
+    return
+  }
 
+  frameIds[index] = window.requestAnimationFrame(() => {
+    frameIds[index] = 0
+
+    const element = cardRefs.value[index]
+
+    if (element) {
+      element.style.transform = pendingTransforms[index]
+    }
+  })
+}
+
+const cacheCardRect = (event, index) => {
+  if (!supportsInteractiveTilt) {
+    return
+  }
+
+  cardRects[index] = event.currentTarget.getBoundingClientRect()
+}
+
 const updateCardTilt = (event, index) => {
+  if (!supportsInteractiveTilt) {
+    return
+  }
+
   const { currentTarget, clientX, clientY } = event
-  const rect = currentTarget.getBoundingClientRect()
+  const rect = cardRects[index] ?? currentTarget.getBoundingClientRect()
   const offsetX = (clientX - rect.left) / rect.width - 0.5
   const offsetY = (clientY - rect.top) / rect.height - 0.5
   const rotateY = offsetX * 10
@@ -85,19 +131,42 @@
   const translateX = offsetX * 8
   const translateY = offsetY * 8
 
-  cardTransforms.value[index] =
+  cardRects[index] = rect
+  pendingTransforms[index] =
     `translate3d(${translateX}px, ${translateY}px, 22px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale(1.02)`
+  scheduleTransformUpdate(index)
 }
 
 const resetCardTilt = (index) => {
-  cardTransforms.value[index] = defaultTransform
+  if (!supportsInteractiveTilt) {
+    return
+  }
+
+  cardRects[index] = null
+  pendingTransforms[index] = defaultTransform
+  scheduleTransformUpdate(index)
 }
+
+onMounted(() => {
+  supportsInteractiveTilt = window.matchMedia('(hover: hover) and (pointer: fine)').matches
+})
+
+onBeforeUnmount(() => {
+  frameIds.forEach((frameId) => {
+    if (frameId) {
+      window.cancelAnimationFrame(frameId)
+    }
+  })
+})
 </script>
 
 <style scoped>
 .advantages {
   width: 100%;
   margin-bottom: 50px;
+  content-visibility: auto;
+  contain: layout paint style;
+  contain-intrinsic-size: 760px;
 }
 
 .advantages__video {
@@ -164,6 +233,7 @@
     border-color 0.18s ease,
     background 0.18s ease;
   will-change: transform;
+  transform: translate3d(0px, 0px, 0px) rotateX(0deg) rotateY(0deg) scale(1);
 }
 
 .advantages__card--wide {
--- .codex-backups/home-performance-20260401/HomeArticlesSection.vue	2026-04-01 12:57:46
+++ src/pages/home/components/HomeArticlesSection.vue	2026-04-01 12:59:17
@@ -108,6 +108,9 @@
   margin-bottom: 50px;
   padding: 36px 0;
   z-index: 0;
+  content-visibility: auto;
+  contain: layout paint style;
+  contain-intrinsic-size: 980px;
 }
 
 .articles::before {
--- .codex-backups/home-performance-20260401/HomeFooterSection.vue	2026-04-01 12:57:46
+++ src/pages/home/components/HomeFooterSection.vue	2026-04-01 12:59:21
@@ -33,6 +33,9 @@
   font-size: 15px;
   padding: 3px 0 30px;
   border-top: 1px solid color-mix(in srgb, var(--cyan) 22%, var(--white));
+  content-visibility: auto;
+  contain: layout paint style;
+  contain-intrinsic-size: 220px;
 }
 
 .footer::before {
